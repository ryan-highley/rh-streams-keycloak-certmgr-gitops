apiVersion: v1
kind: Service
metadata:
  name: '{{ .Values.deployment.name }}'
spec:
  type: ClusterIP
  ports:
  - name: dex
    port: 5556
    protocol: TCP
    targetPort: http
  selector:
    app: '{{ .Values.deployment.name }}'
