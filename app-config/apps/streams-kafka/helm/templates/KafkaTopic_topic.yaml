{{- range .Values.topics }}
---
apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: {{ .name }}
  labels:
    strimzi.io/cluster: {{ $.Values.cluster.name }}
spec:
  {{- toYaml .spec | nindent 2 }}
{{- end }}
