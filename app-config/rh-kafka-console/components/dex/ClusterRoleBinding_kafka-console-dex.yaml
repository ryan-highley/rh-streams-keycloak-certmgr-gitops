apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: console-dex
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: console-dex
subjects:
- kind: ServiceAccount
  name: openshift-oidc-provider-sa    # Service account assigned to the dex pod
  namespace: kafka-console-dex        # The namespace dex is running in
