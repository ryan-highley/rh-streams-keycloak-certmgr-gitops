{{- with .Values.tls }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .secret }}
spec:
  issuerRef:
    {{- toYaml .issuerRef | nindent 4 }}
  secretName: {{ .secret }}
  {{- with .certificate }}
  {{ toYaml . | nindent 2 }}
  {{- end }}
{{- end }}
