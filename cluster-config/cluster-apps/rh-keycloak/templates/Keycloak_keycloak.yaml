apiVersion: k8s.keycloak.org/v2alpha1
kind: Keycloak
metadata:
  name: keycloak
  labels:
    app: keycloak
spec:
  {{- with .Values.persistence }}
  db:
    {{ toYaml .db | nindent 4 }}
  {{- end }}
  {{ toYaml .Values.keycloak | nindent 2 }}
