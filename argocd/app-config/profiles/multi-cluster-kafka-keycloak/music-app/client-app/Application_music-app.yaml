apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: music-app
  namespace: openshift-gitops
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: music-app
    server: https://kubernetes.default.svc
  project: music-app
  source:
    path: app-config/apps/kafka-cli-client/overlays/default
    repoURL: https://github.com/ryan-highley/rh-streams-keycloak-certmgr-gitops
    kustomize:
      patches:
        - target:
            kind: Certificate
            name: kafka-client-cert-issuer
          patch: |-
            - op: replace
              path: /spec/commonName
              value: music-app-kafka-client-ca
        - target:
            kind: Certificate
            name: kafka-client-cert
          patch: |-
            - op: replace
              path: /spec/commonName
              value: music-app-kafka-client
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
